{
    "registration": {
        "domain": "{{SERVICE_NAME}}",
        "type": "host",
        "service": {
            "type": "service",
            "service": {
                "srvce": "_minio",
                "proto": "_tcp",
                "port": 9000
            },
            "ttl": 60
        },
        "ttl": 30
    },

    "zookeeper": {
        "servers": [
            {{#ZK_SERVERS}}
                {
                    "host": "{{host}}",
                    "port": {{port}}
                }{{^last}}, {{/last}}
            {{/ZK_SERVERS}}
        ],
        "timeout": 60000
    },
    "adminIp": "{{auto.MANTA_IP}}"
}